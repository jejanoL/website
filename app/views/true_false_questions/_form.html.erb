<%= form_for(@true_false_question) do |f| %>
  <% if @true_false_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@true_false_question.errors.count, "error") %> prohibited this true_false_question from being saved:</h2>

      <ul>
      <% @true_false_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
